version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.7.2
    working_directory: ~/docker_django_sns/myproject
    steps:
      - checkout
      - setup_remote_docker:
          version: 19.03.13
      - run:
          name: test
          command: |
            pwd
            - docker-compose run web sh -c "python manage.py test && flake8"
            #docker-compose -f myproject/docker-compose.yml run web python ./manage.py test