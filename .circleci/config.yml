version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.7.2
    working_directory: ~/docker_django_sns/myproject
    steps:
      - save_cache:
          key: my-cache
          paths:
            - /home/.circleci/docker_django_sns/myproject/manage.py
      - checkout
      - setup_remote_docker:
          version: 19.03.13
      - run:
          name: test
          command: |
            pwd
            ls
            cd myproject
            ls
            #docker-compose build
            docker-compose run python ./manage.py test
            #docker-compose run web python manage.py test
            ＃docker-compose -f myproject/docker-compose.yml run web python manage.py test