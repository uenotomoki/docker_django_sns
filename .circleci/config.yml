version: 2.1
jobs:
  build:
    docker:
      - image:  cimg/node:lts
    steps:
      - checkout
      - run:
          name: docker-compose up
          command: |
            set -x
            ./myproject/docker-compose up --build -d